<nav class="navbar navbar-expand-lg header-color">
  <div class="container-fluid">
  <%= link_to 'MyWeather', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">

        <% if user_signed_in? %>        
            <li class="nav-item">
                <%= button_to("Sign Out", destroy_user_session_path, method: :delete, class:"nav-link active") %>
            </li>
            <li class="nav-item">
                <%= link_to 'New Location', new_location_path, class:"nav-link active"%>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Locations
              </a>
              <ul class="dropdown-menu">
                <% @locations.each do |location| %>

                  <% if location.user_id == current_user.id %>
                    <li>
                      <%= link_to "#{location.city}, #{location.state}", location, class:"dropdown-item"%>
                    </li>
                  <% end %>
                <% end %>

              </ul>
            </li>

        <% else %>
            <li class="nav-item">
                <%= link_to 'Sign up', new_user_registration_path, class:"nav-link active"%>
            </li>
            <li class="nav-item">
                <%= link_to 'Sign in', new_user_session_path, class:"nav-link active"%>
            </li>
        <% end %>

        
        
      </ul>
    </div>
  </div>
</nav>